version: '1.1'
name: Status Check
env:
  host: 36.37.134.139:4000
tests:
  example:
    steps:
      - name: Health Check
        http:
          url: http://${{env.host}}/health-check
          method: GET
          check:
            status: /^20/
